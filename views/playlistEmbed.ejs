<!DOCTYPE html>
<html>

<%- include('./partials/header') %>

    <body class="divEmbed" style="background-color: black; color: white;">
        <% tracks.forEach(track=> { %>
            <div>
                <iframe 
                src="https://open.spotify.com/embed/track/<%= track.id %>" 
                 
                
                frameborder="0" 
                allowtransparency="true" 
                allow="encrypted-media">
                </iframe>
            </div>
        <% }) %>

    </body>

</html>